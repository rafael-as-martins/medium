server:
  port: 8989 #Port the server will be run-in

spring:
  profiles:
    active: native # backend repository that will be used to store the configuration
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/rafael_as_martins/Documents/projects/medium/configuration-repo/src/main/resources/config
          # backend repository that will be used to store the configuration







